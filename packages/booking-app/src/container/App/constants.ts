const prefix = 'app';

export const INIT_DATA = `${prefix}/INIT_DATA`;
export const LOAD_COOKIE = `${prefix}/LOAD_COOKIE`;
export const CLEAR = `${prefix}/CLEAR`;

export const SET_LOADING = `${prefix}/SET_LOADING`;

export const CHECK_TOKEN = `${prefix}/CHECK_TOKEN`;
export const SET_TOKEN = `${prefix}/SET_TOKEN`;
export const LOG_IN = `${prefix}/LOG_IN`;
export const LOG_OUT = `${prefix}/LOG_OUT`;
